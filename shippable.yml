

# Language setting
language: node_js

# Version number
node_js:
  - 0.10
  - "0.12"
  
env:
  
  matrix:
    - M1=ONE
    - M2=TWO

    
build:
 # pre_ci:
 #   - docker build -t=shippabledocker/sample_node .

  ci:
    - ps -eax
    - ls -al


integrations:
  notifications:
    - integrationName: irc
      type: irc
      recipients:
        - "chat.freenode.net#madrasman"
        - "chat.freenode.net#madrasman2"
      on_success: always
      on_failure: change
      on_start: always    
    
