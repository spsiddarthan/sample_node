language: 
  - node_js
  - python 
node_js:
 # - 0.12
  - 0.10.25

env:
 global:
   - M1=ONE
   - M2=two

services:
  - neo4j


  
build:
  
  ci:
    - ps -eax
    - ls -al
    - mysql -e "create database if not exists \`project-test\`;"
    - shippable_retry 'if [ $i -eq 3 ]; then echo "done"; else foobar$i; fi'
    - npm install
    - npm test
    - echo $M1 and $M2
    - npm install
    - npm test
    - if [ "$SHIPPABLE_NODE_VERSION" == "0.10.25" ]; then foobar; fi
    #- sleep 7000s
