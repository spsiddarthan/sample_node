# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE
    - M2=TWO

matrix:
  exclude: 
    - node_js: "5.0"
      env: M1=one
    
    - node_js: "4.2.2"
      env: M2=two
     
build:
  
  ci:
    - ps -eax
    - ls -al
    - echo "hey"
    - echo $M1
    - echo $M2


    
    
    
    
    
