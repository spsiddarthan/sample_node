

# Language setting
language: node_js

# Version number
node_js:
  - 0.10
  - 0.12
  
env:
  global:
    - FOO=FUBU
  matrix:
    - M1=ONE
    - M2=TWO
matrix:
  exclude:
    - node_js : "0.10"
      env : M1=ONE
    
    - node_js : "0.12"
      env : M2=TWO   
  
build:
 # pre_ci:
 #   - docker build -t=shippabledocker/sample_node .

  ci:
    - ps -eax
    - ls -al
    
    
